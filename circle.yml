machine:
  python:
    version:
      3.4.3
dependencies:
  pre:
    - pip3 install -qU --compile pip
  override:
    - pip3 install -qU --compile -r requirements.txt
  post:
    - python setup.py install
test:
  override:
    - make lint
    - make test
